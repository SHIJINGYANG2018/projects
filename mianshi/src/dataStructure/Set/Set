set 继承collection 接口  是不允许出现重复元素，并且无序的集合

 主要  HashSet 和TreeSet 两大实现类

 在判断重复元素 的时候，set 调用hashCode() equal() 来实现


有序集合 和无序集合

有序集合：集合里的元素可以根据key 和value访问  (List Map)
无序集合：集合里的元素只能遍历获取（Set）


@
特点
不允许重复元素
允许插入null值
元素无须  插入的顺序与取出来的顺序不同
线程不安全  如果两个线程同时操作HashSet   必须通过代码实现


HashSet
在添加的时候，判断 value的hash是否存在于数组中

TreeSet   红桃树
特点：
底层使用的红桃树结构，不是哈希表结构

允许插入空
不允许插入重复值
线程不安全
